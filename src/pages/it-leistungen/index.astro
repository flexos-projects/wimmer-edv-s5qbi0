```astro
---
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/Hero.astro';
import Section from '../../components/Section.astro';
import Card from '../../components/Card.astro';
import CTA from '../../components/CTA.astro';

import servicesHeroImage from '../../assets/images/services-hero.jpg';

const services = await getCollection('services');

const pageTitle = "IT-Leistungen für KMU | Wimmer EDV | St. Pölten";
const pageDescription = "Umfassende IT-Services aus einer Hand: IT-Sicherheit, Cloud-Lösungen, Support und Hardware für Unternehmen in St. Pölten und Umgebung.";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <Hero
    variant="split"
    title="Maßgeschneiderte IT-Lösungen für Ihr Wachstum"
    subtitle="Von proaktiver Wartung bis zu strategischen Cloud-Migrationen – wir stärken Ihr Unternehmen mit zuverlässiger Technologie, damit Sie sich auf Ihr Kerngeschäft konzentrieren können."
    primaryBtnText="Kostenlose Beratung anfordern"
    primaryBtnUrl="/kontakt"
    secondaryBtnText="Warum Wimmer EDV?"
    secondaryBtnUrl="/warum-wir"
  >
    <Image
      src={servicesHeroImage}
      alt="Ein aufgeräumter, moderner Arbeitsplatz mit zwei Monitoren, der die Effizienz durch IT-Lösungen von Wimmer EDV symbolisiert"
      widths={[400, 800, 1200]}
      sizes="(max-width: 768px) 100vw, 50vw"
      format="webp"
      class="w-full h-auto object-cover rounded-2xl shadow-xl aspect-[4/3] md:aspect-square"
    />
  </Hero>

  <Section variant="accent">
    <div class="text-center max-w-3xl mx-auto">
      <h2 class="text-4xl md:text-5xl font-bold text-primary-700">Unsere Leistungsbereiche</h2>
      <p class="mt-4 text-lg text-surface-800">
        Wir bieten ein umfassendes Spektrum an IT-Dienstleistungen, die darauf ausgelegt sind, Ihre Betriebsabläufe zu optimieren, Ihre Sicherheit zu maximieren und Ihr Wachstum zu unterstützen.
      </p>
    </div>

    <div class="mt-16 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {services.map((service, index) => (
        <div data-aos="fade-up" data-aos-delay={index * 100}>
          <Card class="h-full flex flex-col">
            <Image 
              slot="image"
              src={service.data.hero_image} 
              alt={service.data.title}
              class="w-full h-48 object-cover"
              widths={[200, 400]}
              sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
              format="webp"
            />
            <div slot="content" class="p-6 flex flex-col flex-grow">
              <h3 class="text-2xl font-bold text-primary-700 mb-3">{service.data.title}</h3>
              <p class="text-surface-800 text-base mb-6 flex-grow">{service.data.summary}</p>
              <a href={`/it-leistungen/${service.slug}`} class="text-accent-600 hover:text-accent-500 font-bold inline-flex items-center group mt-auto">
                Mehr erfahren
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="ml-1 w-5 h-5 transform group-hover:translate-x-1 transition-transform duration-200"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              </a>
            </div>
          </Card>
        </div>
      ))}
    </div>
  </Section>

  <CTA
    title="Nicht sicher, was Sie benötigen?"
    subtitle="Jedes Unternehmen ist einzigartig. Lassen Sie uns in einem unverbindlichen Gespräch herausfinden, welche IT-Lösungen für Sie den größten Mehrwert schaffen. Wir beraten Sie gerne."
    buttonText="Jetzt Beratungstermin vereinbaren"
    buttonUrl="/kontakt"
  />
</BaseLayout>
```