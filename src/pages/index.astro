```astro
---
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import Section from '../components/Section.astro';
import Card from '../components/Card.astro';
import CTA from '../components/CTA.astro';
import { Icon } from 'astro-icon/components';

// --- Data Fetching ---
const services = await getCollection('services');
const featuredTestimonials = await getCollection('testimonials', ({ data }) => data.is_featured);
const certifications = await getCollection('certifications');

// --- Asset Imports ---
// Assuming placeholder image assets exist at these paths
import whyUsImage from '../assets/images/content/why-us-snapshot.jpg';
import founderImage from '../assets/images/people/thomas-wimmer.jpg';

const pageTitle = "Wimmer EDV | Ihr Gold-Zertifizierter IT-Partner in St. Pölten";
const pageDescription = "Zuverlässige IT-Betreuung für KMU in St. Pölten. Microsoft Gold Partner, 5-Sterne-Bewertungen. Sichern Sie sich jetzt Ihre kostenlose Erstberatung.";
---

<BaseLayout title={pageTitle} description={pageDescription}>

  <!-- 1. Hero Section -->
  <Hero
    variant="impact"
    backgroundImage="/images/hero/home-hero.jpg"
    headline="IT, auf das Sie sich verlassen können."
    subheadline="Ihr Gold-Zertifizierter IT-Partner in St. Pölten. Persönlich, proaktiv und immer für Sie da."
    ctaText="Kostenlose Beratung anfordern"
    ctaHref="/kontakt"
  >
    <div slot="trust-elements" class="mt-8 md:mt-12 flex flex-wrap justify-center items-center gap-x-8 gap-y-4 text-white">
      <div class="flex items-center gap-2">
        <Icon name="lucide:star" class="w-5 h-5 text-secondary-400" />
        <span class="text-sm font-medium">5-Sterne Bewertungen</span>
      </div>
      {certifications.slice(0, 2).map(cert => (
        <div class="flex items-center gap-3 opacity-90">
          <Image
            src={cert.data.logo}
            alt={`${cert.data.partner_name} ${cert.data.level} Logo`}
            width={32}
            height={32}
            loading="eager"
            class="h-8 w-auto object-contain"
          />
          <span class="text-sm font-medium">{cert.data.partner_name} {cert.data.level}</span>
        </div>
      ))}
    </div>
  </Hero>

  <!-- 2. Social Proof Bar -->
  <Section variant="accentuated" class="py-12 md:py-16">
    <div class="mx-auto max-w-7xl">
      <h2 class="text-center text-lg font-semibold leading-8 text-surface-800">
        Vertraut von führenden Unternehmen und zertifiziert von den Besten der Branche
      </h2>
      <div class="mx-auto mt-10 grid max-w-lg grid-cols-4 items-center gap-x-8 gap-y-10 sm:max-w-xl sm:grid-cols-6 sm:gap-x-10 lg:mx-0 lg:max-w-none lg:grid-cols-5">
        {certifications.map(cert => (
          <Image
            src={cert.data.logo}
            alt={`${cert.data.partner_name} Logo`}
            width={158}
            height={48}
            class="col-span-2 max-h-12 w-full object-contain lg:col-span-1 filter grayscale opacity-60 hover:grayscale-0 hover:opacity-100 transition-all duration-300 ease-in-out"
          />
        ))}
      </div>
    </div>
  </Section>


  <!-- 3. Services Overview -->
  <Section id="services">
    <div class="text-center">
      <p class="font-semibold text-accent-500">Unsere Leistungen</p>
      <h2 class="mt-2 text-4xl font-bold tracking-tight text-primary-700 sm:text-5xl">
        Umfassende IT-Lösungen aus einer Hand
      </h2>
      <p class="mt-6 max-w-2xl mx-auto text-lg leading-8 text-surface-600">
        Wir bieten alles, was Ihr Unternehmen für eine reibungslose, sichere und zukunftsfähige IT-Infrastruktur benötigt.
      </p>
    </div>
    <div class="mt-20 grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-4">
      {services.map(service => (
        <Card variant="iconTop" href={`/it-leistungen/${service.slug}`}>
          <div slot="icon" class="mb-6 flex h-14 w-14 items-center justify-center rounded-full bg-primary-100 text-primary-600">
            <Icon name={service.data.icon || 'lucide:settings'} class="h-8 w-8" />
          </div>
          <h3 slot="title" class="text-xl font-bold text-primary-700">{service.data.title}</h3>
          <p slot="content" class="mt-2 text-base text-surface-600">{service.data.summary}</p>
        </Card>
      ))}
    </div>
  </Section>

  <!-- 4. "Why Us?" Snapshot -->
  <Section variant="accentuated">
    <div class="grid grid-cols-1 items-center gap-x-16 gap-y-12 lg:grid-cols-2">
      <div class="text-center lg:text-left">
        <p class="font-semibold text-accent-500">Der Wimmer EDV Unterschied</p>
        <h2 class="mt-2 text-4xl font-bold tracking-tight text-primary-700 sm:text-5xl">
          Ihr Erfolg ist unser Antrieb.
        </h2>
        <p class="mt-6 text-lg leading-8 text-surface-600">
          Wir sind mehr als nur ein Dienstleister – wir sind Ihr strategischer Partner, der den Wert von stabiler und sicherer IT für Ihr Unternehmen versteht.
        </p>
        <ul class="mt-8 space-y-4 text-surface-600 text-left">
          <li class="flex items-start gap-3">
            <Icon name="lucide:user-check" class="h-6 w-6 flex-shrink-0 text-accent-500 mt-1" />
            <span><strong class="font-semibold text-primary-700">Persönlich & Verantwortlich:</strong> Als inhabergeführtes Unternehmen steht Thomas Wimmer persönlich für die Qualität unserer Arbeit.</span>
          </li>
          <li class="flex items-start gap-3">
            <Icon name="lucide:award" class="h-6 w-6 flex-shrink-0 text-accent-500 mt-1" />
            <span><strong class="font-semibold text-primary-700">Gold-Standard Expertise:</strong> Höchste Zertifizierungen von Microsoft und Veeam garantieren Ihnen erstklassiges Know-how.</span>
          </li>
          <li class="flex items-start gap-3">
            <Icon name="lucide:thumbs-up" class="h-6 w-6 flex-shrink-0 text-accent-500 mt-1" />
            <span><strong class="font-semibold text-primary-700">Bewiesen & Gelobt:</strong> Unsere 5-Sterne-Bewertungen sprechen für sich. Wir liefern, was wir versprechen.</span>
          </li>
        </ul>
         <a href="/warum-wir" class="mt-10 inline-block rounded-lg bg-accent-500 px-6 py-3 text-base font-semibold text-white shadow-sm transition-transform duration-200 hover:scale-105 hover:bg-accent-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-500">
          Erfahren Sie mehr
        </a>
      </div>
      <Image
        src={whyUsImage}
        alt="Ein professionelles Team arbeitet in einem modernen Büro zusammen."
        class="w-full rounded-2xl shadow-xl"
        widths={[400, 600, 800]}
        sizes="(max-width: 768px) 100vw, 50vw"
      />
    </div>
  </Section>

  <!-- 5. Featured Testimonial -->
  {featuredTestimonials.length > 0 && (
    <Section>
      <div class="relative isolate">
        <div class="mx-auto max-w-4xl text-center">
           <figure class="mt-10">
            <blockquote class="text-center text-xl font-semibold leading-8 text-surface-800 sm:text-2xl sm:leading-9">
              <p>“{featuredTestimonials[0].data.quote}”</p>
            </blockquote>
            <figcaption class="mt-10">
              <div class="mt-4 flex items-center justify-center space-x-3 text-base">
                <div class="font-semibold text-primary-700">{featuredTestimonials[0].data.author_name}</div>
                <svg viewBox="0 0 2 2" width="3" height="3" aria-hidden="true" class="fill-surface-400">
                  <circle cx="1" cy="1" r="1" />
                </svg>
                <div class="text-surface-600">{featuredTestimonials[0].data.author_company}</div>
              </div>
            </figcaption>
          </figure>
        </div>
      </div>
    </Section>
  )}


  <!-- 6. Meet the Founder -->
  <Section variant="accentuated">
    <div class="grid grid-cols-1 items-center gap-x-16 gap-y-12 lg:grid-cols-2">
      <Image
        src={founderImage}
        alt="Thomas Wimmer, Gründer von Wimmer EDV"
        class="w-full rounded-2xl shadow-xl order-1 lg:order-2"
        widths={[400, 600, 800]}
        sizes="(max-width: 768px) 100vw, 50vw"
      />
      <div class="text-center lg:text-left order-2 lg:order-1">
        <p class="font-semibold text-accent-500">Ihr persönlicher Ansprechpartner</p>
        <h2 class="mt-2 text-4xl font-bold tracking-tight text-primary-700 sm:text-5xl">
          Thomas Wimmer
        </h2>
        <p class="mt-6 text-lg leading-8 text-surface-600">
          Mit über 10 Jahren Erfahrung in der IT-Branche habe ich Wimmer EDV gegründet, um kleinen und mittleren Unternehmen den verlässlichen, persönlichen und kompetenten IT-Service zu bieten, den sie verdienen. Ihr Erfolg ist mein persönliches Anliegen.
        </p>
        <a href="/ueber-uns" class="mt-10 inline-block rounded-lg bg-accent-500 px-6 py-3 text-base font-semibold text-white shadow-sm transition-transform duration-200 hover:scale-105 hover:bg-accent-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-500">
          Mehr über uns
        </a>
      </div>
    </div>
  </Section>

  <!-- 7. Final CTA -->
  <CTA
    headline="Bereit für IT, die einfach funktioniert?"
    subheadline="Kontaktieren Sie uns noch heute für eine unverbindliche und persönliche Beratung. Entdecken Sie, wie wir Ihr Unternehmen voranbringen können."
    buttonText="Jetzt kostenlose Beratung anfordern"
    buttonHref="/kontakt"
  />

</BaseLayout>
```